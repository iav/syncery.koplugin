# macOS
.DS_Store

# Build artifacts
*.zip
*.tar.gz
.build/

# Python tooling (tools/i18n.py)
__pycache__/
*.pyc

# Development copies (pristine baselines / diffs)
*.pristine

# Editor/linter scratch copies (e.g. foo.lua.tmp.NNNN.hash) — never committed
*.tmp.*

# Test artifact recreated by the spec runner (never committed)
/syncery/
